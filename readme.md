# Gatsby flatlify example

# How to start:

- install gatsby & flatlify dependencies

  `cd ./gatsby && npm i && cd ../flatlify-react-admin-example && npm i && cd ..`

- create .env file like env.example in gatsby folder

- npm run dev
